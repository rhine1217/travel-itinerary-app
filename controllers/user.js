const User = require('../models/user')

module.exports = {
    index,
    updateTrips,
}

function index(req, res) {
    // show User Profile on a page? 
}

function updateTrips(req, res) {
    // push to the array of trips with the trip Id? 
}

